
const sumaNumeros = (a, b, c) => {
    if(!Number.isInteger(a) || !Number.isInteger(b) || !Number.isInteger(c)){
        return "No se permiten números no enteros"
    }

    let resultado = 0
    suma = (a + b + c)

    return suma
}

console.log(sumaNumeros(2, 5, 0))
console.log(sumaNumeros(19, 9, 29))